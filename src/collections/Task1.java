package collections;

import java.util.ArrayList;
import java.util.List;

public class Task1 {
    static List<Integer>
    getDivadedByThtee(List<Integer> numbers) {
        List<Integer> elements = new ArrayList<>();
        for (Integer value : numbers) {
            if (value % 3 == 0) ;
        }
        return elements;
    }
}
